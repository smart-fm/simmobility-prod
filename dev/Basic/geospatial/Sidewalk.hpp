#pragma once

#include <vector>

#include "Node.hpp"
#include "Lane.hpp"

namespace sim_mob
{

/**
 * Pavement subclass for representing sidewalks with no corresponding road segment. Note that
 * typical sidewalks (those attached to a RoadSegment) will likely be represented using a
 * different method, since those are the most common type.
 *
 * \note
 * This is a skeleton class. All functions are defined in this header file.
 * When this class's full functionality is added, these header-defined functions should
 * be moved into a separate cpp file.
 */
class Sidewalk : public Pavement {
public:


private:



};





}
