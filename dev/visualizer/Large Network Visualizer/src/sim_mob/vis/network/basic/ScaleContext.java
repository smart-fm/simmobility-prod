package sim_mob.vis.network.basic;

/**
 * Simple class holding the parameters which are used to scale the network.
 * 
 * \author Seth N. Hetu
 */
/*public class ScaleContext {
	public double percent;
	public DPoint origin;
	public DPoint farthestPoint;
	public double canvasWidth;
	public double canvasHeight;
	
	public ScaleContext(double percent, DPoint origin, DPoint farthestPoint, double canvasWidth, double canvasHeight) {
		this.percent = percent;
		this.origin = origin;
		this.farthestPoint = farthestPoint;
		this.canvasWidth = canvasWidth;
		this.canvasHeight = canvasHeight;
	}

	/**
	 * Return true if the two scale contexts have the same scale. 
	 */
//	public boolean sameScale(ScaleContext other) {
//		return percent == other.percent;
//	}
//}
